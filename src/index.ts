export type {
  Annotation,
  OutputDetailLevel,
  StorageAdapter,
  UiAnnotatorInstance,
  UiAnnotatorOptions,
  UiAnnotatorSettings,
} from '@/types';
export { createUiAnnotator, registerUiAnnotatorElement } from '@/core/ui-annotator';
